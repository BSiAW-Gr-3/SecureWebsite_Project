server {
    # Listen on port 8080
    listen 8080;
    root /usr/share/nginx/html;
    include /etc/nginx/mime.types;

    location / {
        # Try to serve a file directly, then try a directory, then fall back to index.html
        try_files $uri $uri/ /index.html;
    }
}
